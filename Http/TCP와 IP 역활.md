### TCP는 신뢰다.

- HTTP Request를 패킷으로 분해한다.
- 3 way hand shawking 을 통해 보내고 받았다고 응답보내고 응답보낸걸 받았다고 다시 보내서 데이터 신뢰를 담당한다.


### IP는 배송을 담당한다.

- 특정 주소로 데이터를 보내는 담당을 한다.
- 이때 MAC 주소를 통하여 ARP 프로토콜을 이용하여 어느 서버로 갈지 알 수 있다.
- 마치 택배가 배송 될 때 최종 목적지는 모르고 어느 집하지로 보내야 되는지만 아는 것처럼 말이다.
