- ls -al /bin
- chmod +x (실행가능)
- /var (변경가능한 디렉토리)
- /usr (유저가 저장한 디렉토리)
- ps aux | grep apache (프로세스 검색)
- sudo updatedb, locate *.log (DB를 뒤짐, find보다 성능 좋음)
- find (명령어 실제 디렉토리 뒤짐)
- whereis (실행파일 뒤짐)
- ls 명령어가 어디서든 실행되는 이유는 $PATH
- control + z (백그라운드), fg(포그라운드), jobs(백그라운드 리스트 보기)
