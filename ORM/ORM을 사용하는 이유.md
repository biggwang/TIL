### ORM을 사용하는 이유

1 DB커넥션 비용이 높다. (쿼리 실행 할 때마다 커넥션 맺음..)
2 DBMS를 바꾸게 됬을 때 SQL을 다시 작성 할 수도 있다. (자바에 String에 SQL 구문을 넣으므로)
3 반복적인 코드가 많다. (try~resouce가 있어서 그남아 다행이지만 없으면  finally 구문에 close 해줘야 함)


이러한 불편한 이유로 ORM 기술이 나오게 된것이다.
