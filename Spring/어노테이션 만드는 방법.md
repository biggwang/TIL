### 만드는 방법

~~~ java
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface MyPageHeader {
 
}
~~~


### 로직
~~~ java
@Slf4j
@Aspect
@Component
public class MyPageHeaderAop {

    @Autowired
    private MemberService memberService;

    @Around("@annotation(com.musinsa.frontweb.common.annotation.MyPageHeader)")
    public Object mypageHeaderHandler(ProceedingJoinPoint joinPoint) throws Throwable {
      //TODO
    }
}
~~~

### 사용방법
~~~ java
@GetMapping(value = "/members/likeMagazine")
@MyPageHeader
public String getLikeMagazine(Model model,
~~~
